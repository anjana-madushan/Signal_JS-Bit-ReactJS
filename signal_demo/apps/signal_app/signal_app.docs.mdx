---
description: 'A signal_app app.'
labels: ['app', 'react']
---

A React application for signal_app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use signal_app 
```

To run the app locally, run the following:

```
bit run signal_app 
```
